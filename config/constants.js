const SUMMARY_LIMITS = {
  DEFAULT_MESSAGE_COUNT: 100,
  MIN_MESSAGE_COUNT: 1,
  MAX_MESSAGE_COUNT: 1000,
  COOLDOWN_MINUTES: 5,
  PROCESSING_ESTIMATE_SECONDS: '10-30',
  TASK_MESSAGE_TTL_SECONDS: 15 * 60,
  RESPONSE_SEGMENT_MAX_LENGTH: 3500,
  RESPONSE_SEGMENT_MAIN_MAX_LENGTH: 3200
};

const AI_LIMITS = {
  MAX_INPUT_CHARS: 50000,
  MAX_INPUT_TOKENS: 6000,
  MAX_OUTPUT_TOKENS: 50000,
  PRIMARY_TIMEOUT_MS: 20 * 1000,
  FALLBACK_TIMEOUT_MS: 30 * 1000
};

const MESSAGE_STORE = {
  MAX_MESSAGES_PER_CHAT: 2000,
  MAX_MESSAGE_AGE_MS: 7 * 24 * 60 * 60 * 1000,
  CLEANUP_INTERVAL_MS: 6 * 60 * 60 * 1000,
  INITIAL_CLEANUP_DELAY_MS: 10 * 1000
};

const CACHE_TTLS = {
  SUMMARY_TTL_SECONDS: 30 * 60,
  SUMMARY_CHECK_PERIOD_SECONDS: 5 * 60,
  STATS_TTL_SECONDS: 10 * 60,
  STATS_CHECK_PERIOD_SECONDS: 2 * 60,
  USER_TTL_SECONDS: 60 * 60,
  USER_CHECK_PERIOD_SECONDS: 10 * 60,
  API_COOLDOWN_MS: 5 * 60 * 1000,
  API_LIMIT_TTL_SECONDS: 10 * 60
};

const RATE_LIMIT = {
  REQUESTS_PER_MINUTE: 10,
  TIME_WINDOW_MS: 60 * 1000,
  MAX_ENTRIES: 10000,
  CLEANUP_INTERVAL_MS: 5 * 60 * 1000
};

const COMMAND_THROTTLE = {
  CLEANUP_INTERVAL_MS: 30 * 1000,
  EXPIRE_AFTER_MS: 5 * 60 * 1000,
  DEFAULT_THROTTLE_MS: 3000
};

const DUPLICATE_GUARD = {
  MAX_UPDATES: 1000,
  CLEANUP_INTERVAL_MS: 5 * 60 * 1000
};

const TASK_QUEUE = {
  RESULT_TTL_MS: 10 * 60 * 1000,
  EXPIRE_TASK_MS: 30 * 60 * 1000
};

const MESSAGE_STATS = {
  REPORT_INTERVAL_MS: 60 * 60 * 1000,
  REPORT_EVERY_MESSAGES: 1000
};

const CHAT_PERMISSION = {
  RESTRICTION_TTL_SECONDS: 6 * 60 * 60
};

const INPUT_LIMITS = {
  MAX_MESSAGE_LENGTH: 10000,
  MAX_COMMAND_ARG_LENGTH: 1000,
  MAX_INPUT_LENGTH: 1000
};

module.exports = {
  SUMMARY_LIMITS,
  AI_LIMITS,
  MESSAGE_STORE,
  CACHE_TTLS,
  RATE_LIMIT,
  COMMAND_THROTTLE,
  DUPLICATE_GUARD,
  TASK_QUEUE,
  MESSAGE_STATS,
  CHAT_PERMISSION,
  INPUT_LIMITS
};
